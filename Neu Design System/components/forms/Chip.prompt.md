Selectable pill for filters, undertones, and tone placements; pass `swatch` to show a leading color dot (e.g. the olive undertone).

```jsx
<Chip selected swatch="var(--tone-olive)">Olive</Chip>
<Chip>Foundation</Chip>
```

Props: `selected`, `swatch`, `size` (`sm`/`md`).

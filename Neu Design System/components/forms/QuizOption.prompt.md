A big tappable answer for the 30-second quiz; `selected` fills olive-soft with an olive ring and check.

```jsx
<QuizOption selected sublabel="Silver looks better on me" onClick={pick}>Cool</QuizOption>
```

Warm text / search field; pass `icon` for a leading glyph and `pill` for the rounded search style. Focus ring is clay.

```jsx
<Input pill placeholder="Search foundations…" icon={<SearchIcon/>} />
```
